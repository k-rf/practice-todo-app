import { TodoCompletedDate } from "./todo-completed-date";

describe("TodoCompletedDate", () => {
    it("should be defined", () => {
        expect(new TodoCompletedDate()).toBeDefined();
    });
});
