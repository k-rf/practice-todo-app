import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        background: {
            default: "linear-gradient(45deg, #12c2e9, #c471ed, #f64f59);",
        },
    },
});
